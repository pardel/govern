# Run /onboard in a knowledge base that already has a grown CLAUDE.md.
# The guard added 2026-07-26 says it must stop and ask rather than
# overwrite, because overwriting throws away every correction that went
# into the file.
COMMAND=onboard
ASK=""
